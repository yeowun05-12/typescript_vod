{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "strict": true, // 타입이 틀렸는지 안 틀렸는지 검사
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "outDir": "dist", // 빌드한 파일들을 어떤 폴더에 생성할 것인지에 대한 설정
    "noUncheckedIndexedAccess": true // 요소에 접근할 때 좀 더 엄격한 타입 검사를 실행한다.
    // "lib":["ES5"] // 어떤 문법을 사용할 것인지 설정할 수 있다. 배열 구조를 사용해서 다수의 값을 지정해줄 수 있다.
    // 해당 버전에 맞는 라이브러리만 사용할 수 있기때문에 필요한 것들이 누락될 수도 있으니, 항상 확인 할 것
    // 잘 모르면 되도록 사용하지 않을 것
  },
  "include": ["src/**/*"] // 어떤 파일을 빌드 할 것인지에 대한 경로 설정
}
